黄书上的代码
def bin_search(key[],k): key:要查找的数组，k要查找的数
  low = 1
  high = n
  while(low<=high):
    mid = (low + high)/2
    if key[mid]== k:
      return mid
    if k>key[mid]:
      low = mid +1
    else:
      high = high-1
  return 0
  
